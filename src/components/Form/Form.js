import React from 'react';

function Form ( props ) {
    return (
        <form>
            <input/>
            <input/>
            <button>Send</button>
        </form>
    );
}

export default Form;